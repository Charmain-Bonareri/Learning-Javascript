//Write reusable Code with Functions
function ourReusableFunction(){ 
    console.log ("Heyya, World") // everything inside the function is run anytime the funcction is called or invoked.

}

ourReusableFunction(); //calling the function using the function name
ourReusableFunction();
ourReusableFunction();
ourReusableFunction();


function myName () {
    console.log("My name is Charmain")
}

myName();


// passing Values to Functions with Arguments

function ourFunction(a,b){
    console.log(a-b);
}

ourFunction(10,5); // outputs 5

// Return a Value from a Function with Return

// Undefined Value returned

//Assignment with a retuerned Value

// Stand in Line

// Boolean Values